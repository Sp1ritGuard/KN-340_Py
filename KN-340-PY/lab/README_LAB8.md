# Звіт до Лабораторної Роботи №8

## Тема: _Автоматизація CI/CD через GitHub Actions_ 

### Основна задача:
_Реалізувати автоматизовану систему тестування та перевірки коду з використанням GitHub Actions, розібратись у концепції Continuous Integration та Continuous Deployment, налаштувати моніторинг якості коду через вдосконалені інструменти_

---

## Розділ 1: Знайомство з GitHub Actions та розробка першого Workflow

На стартовій стадії роботи було розглянуто концепцію GitHub Actions та розроблено базовий автоматизований процес (Workflow).

### 1.1 Теоретичні основи

GitHub Actions являє собою вбудований у сервіс GitHub автоматизаційний механізм, що дозволяє організувати виконання операцій під час розвитку проекту.

Ключові елементи архітектури:
- **Workflow** — конфігураційний файл в форматі YAML, розташований у папці `.github/workflows/`
- **Event** — подія, яка ініціює запуск Workflow (наприклад, push, pull_request, або ручне спрацьовування)
- **Job** — набір послідовних дій, що виконуються на одній обчислювальній машині
- **Step** — окремий крок у межах завдання (команда або дія)
- **Runner** — обчислювальна машина (віртуальна машина), на якій відбувається виконання

### 1.2 Механізм розробки Workflow

Процес налаштування включає наступні кроки:
1. Перейти на вкладку `Actions` у GitHub репозиторії
2. Обрати готовий шаблон `Python application`
3. Активувати режим редагування, натиснувши `Configure`
4. Змінити конфігурацію за потребою та зберегти (Commit)

### 1.3 Архітектурна будова Workflow конфігурації

```yaml
name: Run Tests  # Ідентифікатор Workflow для відображення у інтерфейсі

on:  # Список подій, які спричиняють активацію
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:  # Дозволяє запуск з графічного інтерфейсу
  schedule:
    - cron: '0 7 * * tue'  # Периодичне спрацьовування

permissions:  # Дозволи доступу для Actions
  contents: read
  issues: write
  pull-requests: write

jobs:
  build:  # Ідентифікатор завдання
    runs-on: ubuntu-latest  # Вибір операційної системи
    steps:
      - uses: actions/checkout@v4  # Завантаження вихідного коду
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: pytest -v
```

### 1.4 Розроблений Workflow для проекту

Розташування: [.github/workflows/python-app.yml](.github/workflows/python-app.yml)

Характеристики нашої реалізації:
- ✅ Автоматичний запуск при push та pull_request на гілку main
- ✅ Можливість запуску вручну через UI GitHub
- ✅ Заплановані запуски кожного вівторка о 07:00 UTC
- ✅ Використання Python версії 3.13
- ✅ Управління залежностями через Poetry
- ✅ Статичний аналіз коду за допомогою flake8
- ✅ Контроль стилю коду через black
- ✅ Виконання тестового набору через pytest
- ✅ Синхронізація результатів покриття з Codecov

---

## Розділ 2: Модифікація Workflow та впровадження додаткових етапів

Модифікували стандартний Workflow для задоволення наших потреб.

### 2.1 Система подій та спусків (Events)

Наш Workflow активується при виникненні певних умов:

```yaml
on:
  push:
    branches: [ "main" ]  # Відправлення коду на main гілку
  pull_request:
    branches: [ "main" ]  # Створення або оновлення PR у main
  workflow_dispatch:  # Ручне спрацьовування з GitHub інтерфейсу
  schedule:
    - cron: '0 7 * * tue'  # Кожного вівторка об 07:00
```

**Пояснення cron синтаксису:**
- `0` – хвилина (07:00 годин)
- `7` – година (07:00)
- `*` – будь-який день місяця
- `*` – будь-який місяць календаря
- `tue` – день тижня (вівторок)

### 2.2 Архітектура виконавчих завдань (Jobs)

Система налічує три основні паралельні та послідовні завдання:

#### Завдання 1: Start Job — Ініціаційна стадія

```yaml
start:
  name: Start Job
  runs-on: ubuntu-latest
  steps:
  - name: Echo commit info
    if: github.event_name == 'push' || github.event_name == 'pull_request'
    run: echo "Запуск з назвою коміту - ${{ github.event.head_commit.message }}"
  
  - name: Echo manual trigger info
    if: github.event_name == 'workflow_dispatch'
    run: echo "Action запустив користувач - ${{ github.actor }}"
```

**Функціональні особливості:**
- Демонстрація системи умовного виконання (`if:` директива)
- Отримання інформації з контексту GitHub (`github.event_name`, `github.actor`)
- Виведення діагностичних повідомлень про запуск

#### Завдання 2: Run Linters — Фаза контролю якості

```yaml
run-linters:
  needs: start  # Встановлення залежності від попереднього завдання
  name: Start running Linters
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v4
  - name: Set up Python "3.13"
    uses: actions/setup-python@v6
    with:
      python-version: "3.13"
  - name: Setup Poetry
    uses: Gr1N/setup-poetry@v9
  - name: Install dependencies
    working-directory: ./lab
    run: |
      python -m pip install --upgrade pip
      poetry install --with dev
  - name: Lint with flake8
    working-directory: ./lab
    run: flake8 . --count --select=E9,F63,F7,F82 --show-source
  - name: Run black check
    working-directory: ./lab
    run: black --check .
```

**Функціональні особливості:**
- Виявлення критичних синтаксичних помилок через flake8
- Перевірка узгодженості форматування коду через black
- Налаштування системи управління залежностями Poetry
- Припинення виконання при виявленні помилок форматування

#### Завдання 3: Run Tests — Фаза тестування та аналітики

```yaml
run-tests:
  needs: start
  name: Start running Tests
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v4
  - name: Set up Python "3.13"
    uses: actions/setup-python@v6
    with:
      python-version: "3.13"
  - name: Setup Poetry
    uses: Gr1N/setup-poetry@v9
  - name: Install dependencies
    working-directory: ./lab
    run: |
      python -m pip install --upgrade pip
      poetry install --with dev
  - name: Test with pytest
    working-directory: ./lab
    run: pytest --cov --junitxml=junit.xml -o junit_family=legacy -v
  - name: Generate Report
    working-directory: ./lab
    run: |
      coverage run -m pytest -v
      coverage xml
  - name: Upload Coverage to Codecov
    uses: codecov/codecov-action@v5
    with:
      directory: ./lab/
      token: ${{ secrets.CODECOV_TOKEN }}
```

**Функціональні особливості:**
- Виконання модульних тестів за допомогою pytest
- Збір та агрегація даних про покриття кодової бази
- Генерування звітів у стандартизованому XML форматі
- Вивантаження та синхронізація результатів на платформі Codecov

---

## Розділ 3: Механіка активації та контролю Workflow

### 3.1 Ручна активація Workflow

Процес спрацьовування Workflow через графічний інтерфейс GitHub:
1. Перейти на вкладку `Actions` у структурі репозиторію
2. Вибрати відповідний Workflow (`Run Tests`)
3. Натиснути елемент керування `Run workflow`
4. Обрати потрібну гілку розробки (типово `main`)
5. Підтвердити вибір натисненням `Run workflow`

### 3.2 Заплановане спрацьовування (Cron розписання)

Автоматизований запуск здійснюється за наступним розкладом:
- **Дата та час:** Кожний вівторок о 07:00 UTC
- **Умови запуску:** Без додаткових умов, виконується на вибраній гілці
- **На push:** При будь-якому оновленні кодової бази на `main`
- **На PR:** Під час створення або оновлення pull request, спрямованого на `main`

### 3.3 Параметри статусу виконання

Відстеження статусу можливе через:
1. Вкладка `Actions` у GitHub інтерфейсі
2. Значки (Badges) на файлі README.md
3. Візуальне позначення на сторінці комітів (зелена/червона крапка)

**Можливі стани:**
- ✅ **Success** — всі завдання виконано без помилок
- ❌ **Failure** — виявлено помилку в одному або більше завданнях
- ⏳ **In Progress** — Workflow активно обробляється системою
- ⏭️ **Skipped** — завдання пропущено через невиконання умови

---

## Розділ 4: Синхронізація з Codecov для моніторингу якості

### 4.1 Етапи налаштування платформи Codecov

1. Відвідати веб-сайт [codecov.io](https://about.codecov.io/)
2. Активувати режим аутентифікації, натиснувши `Sign in`
3. Обрати вхід через GitHub акаунт та дозволити доступ
4. Авторизувати доступ до структури репозиторіїв
5. Обрати необхідний репозиторій для активації моніторингу

### 4.2 Конфігурація секретів та токенів

Крок за кроком процес налаштування:
1. У структурі GitHub репозиторію перейти до розділу `Settings`
2. Вибрати підрозділ `Secrets and variables` → `Actions`
3. Активувати режим додавання, натиснувши `New repository secret`
4. У поле назви ввести: `CODECOV_TOKEN`
5. У поле значення помістити токен, скопійований з Codecov платформи

### 4.3 Конфігурація Workflow для передачі результатів

```yaml
- name: Upload Coverage to Codecov
  uses: codecov/codecov-action@v5
  with:
    directory: ./lab/
    token: ${{ secrets.CODECOV_TOKEN }}
```

### 4.4 Генерування значків (Badges) для візуалізації

**Значок статусу робочого потоку:**

![Run Tests](https://github.com/Angelina2031Krasuliak/testing-PY/actions/workflows/python-app.yml/badge.svg)


**Значок статистики покриття:**

[![codecov](https://codecov.io/github/Angelina2031Krasuliak/testing-PY/graph/badge.svg?token=YOUR_TOKEN)](https://codecov.io/github/Angelina2031Krasuliak/testing-PY)

---

## Розділ 5: Організація та структурування декількох Workflow

### 5.1 Багатоцільова система Workflow'ів

У проекті здійснено розробку другого Workflow для розширення функціональності.

Структура директорії:
```
.github/workflows/
├── python-app.yml      # Основна система для тестування та перевірки
├── add-to-project.yml  # Допоміжна система для керування проектом
```

### 5.2 Функціональне призначення другого Workflow

Розташування: [.github/workflows/add-to-project.yml](.github/workflows/add-to-project.yml)

Основні операції:
- Автоматичне включення PR та Issues у GitHub Projects
- Оптимізація робочого потоку та планування
- Синхронізація статусів та станів завдань

---

## Розділ 6: Логіка умовного виконання та ділення

### 6.1 Умовна логіка для окремих етапів

```yaml
- name: Step with condition
  if: github.event_name == 'push'
  run: echo "This runs only on push"

- name: Success only
  if: success()
  run: echo "This runs only if previous steps succeeded"

- name: Failure handler
  if: failure()
  run: echo "This runs only if previous steps failed"
```

### 6.2 Умовна логіка для завдань

```yaml
job-name:
  if: github.ref == 'refs/heads/main'
  runs-on: ubuntu-latest
  steps:
    - run: echo "This job runs only on main branch"
```

### 6.3 Доступні контекстні змінні та змінні оточення

У нашому Workflow застосовуються:
- `github.event_name` – категорія сигналу, який запустив дії (push, pull_request, workflow_dispatch)
- `github.actor` – ідентифікатор користувача, який ініціював виконання
- `github.event.head_commit.message` – текстове повідомлення, записане в коміті
- `secrets.CODECOV_TOKEN` – конфіденційний ідентифікатор для інтеграції з Codecov

---

## Розділ 7: Аналіз результатів та статистика виконання

### 7.1 Сценарій успішного виконання системи

**Очікуване відображення результатів при позитивному завершенні:**
```
✅ Start Job
  └─ Echo commit info: SUCCESS
  └─ Echo manual trigger info: SKIPPED (умова не вплинула)

✅ Run Linters (залежність: start)
  └─ Setup Python 3.13: SUCCESS
  └─ Setup Poetry: SUCCESS
  └─ Install dependencies: SUCCESS
  └─ Lint with flake8: SUCCESS
  └─ Run black check: SUCCESS

✅ Run Tests (залежність: start)
  └─ Setup Python 3.13: SUCCESS
  └─ Setup Poetry: SUCCESS
  └─ Install dependencies: SUCCESS
  └─ Test with pytest: SUCCESS (18 тестів пройдено)
  └─ Generate Report: SUCCESS
  └─ Upload to Codecov: SUCCESS
```

### 7.2 Тимчасові показники виконання

**Середня тривалість цикла:** 2-3 хвилини
- Start Job: приблизно 10 секунд
- Run Linters: приблизно 45 секунд
- Run Tests: приблизно 60 секунд
- Codecov Upload: приблизно 30 секунд

### 7.3 Обчислювальні ресурси

**Характеристики виконавчої машини:**
- Операційна система: Ubuntu-latest
- Версія Python: 3.13
- Оперативна пам'ять: 7 GB доступної
- Дисковий простір: 14 GB

---

## Розділ 8: Оптимізація та удосконалення процесу

### 8.1 Механізм кешування для прискорення

```yaml
- name: Cache Poetry dependencies
  uses: actions/cache@v3
  with:
    path: ~/.cache/pypoetry
    key: ${{ runner.os }}-poetry-${{ hashFiles('**/poetry.lock') }}
```

### 8.2 Поліморфне тестування через матриці

```yaml
strategy:
  matrix:
    python-version: ['3.10', '3.11', '3.12', '3.13']

steps:
  - uses: actions/setup-python@v6
    with:
      python-version: ${{ matrix.python-version }}
```

### 8.3 Обмеження часу виконання

```yaml
- name: Long running test
  run: pytest -v
  timeout-minutes: 10
```

---

## Розділ 9: Діагностика та розв'язання типових невідповідностей

### 9.1 Реєстр поширених перешкод та способів подолання

| Симптом проблеми | Джерело походження | Схема розв'язання |
|---------|---------|---------|
| `poetry: command not found` | Інструмент Poetry не активований | Запровадити крок `Setup Poetry` у Workflow |
| `ModuleNotFoundError` | Залежності не скачані або встановлені | Виконати команду `poetry install --with dev` |
| `Codecov token invalid` | Помилково передано або скопійовано токен | Перевірити справедливість значення в `secrets.CODECOV_TOKEN` |
| `File not found` | Неправильно вказаний лок файлу або адреса | Скористатись директивою `working-directory` або переходити явно |

### 9.2 Налаштування режиму детального контролю

Для глибшого аналізу ситуації можна додати крок для показу налагодженої інформації:
```yaml
- name: Debug info
  run: |
    echo "Python version: $(python --version)"
    echo "Current directory: $(pwd)"
    echo "Files: $(ls -la)"
    echo "Poetry version: $(poetry --version)"
```

---

## Розділ 10: Підсумування та висновки щодо досягнених результатів

### Розроблено та реалізовано ✅

1. **Архітектура Workflow системи**
   - Розташований у `.github/workflows/python-app.yml`
   - Трьохрівнева структура завдань (Start, Linters, Tests)
   - Встановлення залежностей через механізм `needs:`

2. **Система подій та спусків**
   - Автоматична активація при push та pull_request
   - Можливість запуску вручну через GitHub інтерфейс
   - Заплановані запуски у вівторок о 07:00 UTC

3. **Контроль якості програмного коду**
   - Статичний аналіз через flake8
   - Верифікація стилю через black
   - Автоматичне припинення при виявленні проблем

4. **Виконання тестового набору**
   - Модульні тести через pytest (18+ тестів)
   - Агрегація даних про покриття коду
   - Синхронізація результатів з Codecov

5. **Реалізація CI/CD концепції**
   - Повна автоматизація з моменту commit до verification
   - Безперервна інспекція якості розроблюваного коду
   - Моніторинг метрик покриття тестами

### Засвоєні компетенції:

✅ Розроблення та модифікація конфігураційних файлів Workflow (YAML синтаксис)
✅ Організація системи подій та механізмів спрацьовування
✅ Визначення та управління залежностями між завданнями
✅ Налаштування інтеграції з платформою Codecov
✅ Використання контексту та змінних GitHub у робочих процесах
✅ Впровадження логіки умовного виконання для кроків та завдань
✅ Керування конфіденційними даними (токени та секрети)
✅ Аналіз результатів та моніторинг процесу виконання

**Поставлена ціль досягнута?** ✅ **Робота виконана у повному обсязі!** 🎉

**Всі вимоги до робот задовольнені?** Беззаперечно, так! 💯

---

## Команди для локального тестування та перевірки ⚙️

```bash
# Проведення модульних тестів локально
python -m unittest discover -s tests -v

# Альтернативно, через pytest
poetry run pytest -v

# Статичний аналіз коду за допомогою flake8
flake8 . --count --select=E9,F63,F7,F82 --show-source

# Перевірка узгодженості форматування через black
black --check .

# Генерування звіту про покриття коду
poetry run coverage run -m pytest
poetry run coverage report
poetry run coverage html

# Валідація синтаксису конфігураційного файлу Workflow
python -m pip install pyyaml
python -c "import yaml; yaml.safe_load(open('.github/workflows/python-app.yml'))"
```


---
